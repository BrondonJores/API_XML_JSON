# Configuration des bases de données
# IMPORTANT: Générez des mots de passe forts et sécurisés !
MYSQL_ROOT_PASSWORD=CHANGEZ_MOI_AVEC_VALEUR_SECURISEE

# Clés d'application
# Laravel APP_KEY - Générez avec: php artisan key:generate
APP_KEY=base64:CHANGEZ_MOI_AVEC_CLE_GENEREE

# Secrets JWT
# Générez avec: openssl rand -hex 64
JWT_SECRET=CHANGEZ_MOI_AVEC_SECRET_JWT_SECURISE

# Clé secrète Flask Python
# Générez avec: openssl rand -hex 32
SECRET_KEY=CHANGEZ_MOI_AVEC_SECRET_FLASK_SECURISE

# Instructions de génération de secrets sécurisés :
# 
# Pour MYSQL_ROOT_PASSWORD :
#   openssl rand -base64 32
#
# Pour APP_KEY (Laravel) :
#   cd service-gateway-laravel && php artisan key:generate --show
#
# Pour JWT_SECRET :
#   openssl rand -hex 64
#
# Pour SECRET_KEY (Python) :
#   openssl rand -hex 32
